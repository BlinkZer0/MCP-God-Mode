# Macro Run Tool

## Overview
Execute a saved macro with optional variable substitution and dry-run capability (MCP Web UI Bridge).

## Parameters
- `macroId`
- `variables`
- `dryRun`

## Notes
Pairs with `macro_record`. See the catalog for usage patterns.

