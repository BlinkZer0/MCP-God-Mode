import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
// import { registerAudioEditor } from "./audio_editor.js"; // Temporarily disabled

export function registerAudioEditing(server: McpServer) {
  // registerAudioEditor(server); // Temporarily disabled
  console.log("Audio editing tools temporarily disabled due to build issues");
}